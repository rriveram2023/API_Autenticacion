# Certificados TLS para el modo soportado

Usa esta carpeta cuando el repo termina TLS por si mismo con `docker-compose.yml`.

Archivos esperados:

- `fullchain.crt`
- `private.key`

Esta carpeta queda en el repo solo como placeholder.
El contenido real de los certificados debe permanecer local en la VM y no debe versionarse.
